service dr
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = login
    bind        = 0.0.0.0
    server      = /home/dr/dirtyroom
    type        = UNLISTED
    port        = 20210
}
