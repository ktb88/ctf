service onmyway
{
    disable     = no
    flags       = REUSE
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = onmyway
    bind        = 0.0.0.0
    server      = /home/onmyway/challenge.py
    type        = UNLISTED
    port        = 31337
}
